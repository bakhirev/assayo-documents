创建HTML报告以分析提交的统计数据：
- 工作节奏和加班时间数量；
- 责任区域、功能数量和错误；
- 同事的工作风格；
- 员工流动率和团队构成；
- 开发人员的位置；
- 发布日程和假期日程；
- 功能和项目整体的成本；
- 需要重构的地方、已删除的文件等。

链接：
视频：
目录
提交统计报告
如何创建和查看报告？
使用公共服务器
访问[网站]({url})
按照说明
执行`{code}`
打开`{file}`
使用NodeJS库
使用PHP库
使用Python库
使用Ruby库
使用Go库
使用源代码
下载此存储库
将文件`{file}`放在`{dir}`中
或将文件夹`{dir}`放在您的存储库中（`{file}`所在的位置）。您可以更改名称。例如，从`{dir}`更改为`{to}`。
在这种情况下，重要的是文件`{file}`通过命令生成以进行离线查看。
使用GitHub动作
在文件夹`{dir}`中添加[脚本]({url})或使用市场上的此[动作]({action})。一个准备好的和更新的报告将保存在artifact中。
使用私有服务器
下载[docker]({url})镜像；
在本地网络中运行；
使用Web界面查看报告，在URL参数中设置数据URL`{param}`：
容器assayo的URL，监听端口{port}；
Git日志的容器URL；
默认情况下，镜像将在`{url}`中运行。如果不起作用，请检查端口{port}是否可用。
如何连接作者？
在项目的根目录中，您需要创建一个文件`{file}`。
文件内容示例：
了解有关此文件格式的更多信息[这里]({url})。
如何将Git数据导出到txt文件？
在线查看
在项目的根目录中执行：
离线查看
Git将创建一个文件`{file}`。此文件包含显示报告的数据。在线和离线格式之间的区别是字符串的包装。离线格式将作为文件`{ext}`加载，如果您只打开`{report}`
如果您在Windows上使用PowerShell
默认情况下，输出编码可能不符合UTF-8，并且生成的日志文件将无法读取。在保存日志之前，您可以使用命令更改编码。
或者打开保存的文件并手动将编码更改为UTF-8。
如何查看微服务组的报告？
为每个微服务文件`{file}`（{files}等）生成。您可以手动执行，或者使用[Assayo Crawler]({url})模块进行自动日志收集；
查看“如何查看在线报告？”。在最后一步，将所有文件一次性拖放到浏览器窗口中。
查看“如何查看离线报告？”。在第二步，将所有微服务文件（{files}等）拖放到报告文件夹(`{dir}`)中。
项目中的最佳实践
如何签名提交？
遵循[Conventional Commits 1.0.0]({url})。示例：
任务跟踪器中的任务编号`({task})`
工作类型`({type}等)`
功能`({page} - 网站上的新页面或新功能，使用一个（两个）短词或缩写)`
解决了什么问题`({text})`
如何为提交消息添加检查？
使用文件{file}
在文件夹{dir}中创建文件`{file}`
在文件中添加此文本：
使用包[{name}]({url})
使用包{name}
在文件`{file}`中添加属性`{property}`：
运行命令`{code}`
关于此应用程序
如何定制界面？
您可以创建自己的界面主题。选项：
**标题**。您可以在URL参数`{property}`中设置默认文档标题。示例：`{url}`
**可视化主题**。为此，您需要准备一个包含新样式的CSS文件，并在参数`{property}`中指定其URL。示例：`{url}`。您可以使用类名作为选择器。大多数在新版本中不会改变。
**语言**。您可以在URL参数`{property}`中设置语言。示例：`{url}`
**示例**：[demo]({url})
如何从源代码重新构建HTML报告？
下载此存储库`{code}`
新的HTML构建将在`{dir}`文件夹中
如何添加或编辑翻译？
您可以在`{dir}`文件夹中添加新翻译或更正现有翻译。
[指南]({url})
架构
微服务的总体架构
[Reports showcase UI]({url})显示可用报告列表。每个报告包括标题、描述和存储库列表。
[Crawler service]({url})为报告收集存储库日志。
[Log visualization UI]({url})（**您在这里**）显示报告。需要日志文件才能工作。
反馈，评论
优先通信方法
