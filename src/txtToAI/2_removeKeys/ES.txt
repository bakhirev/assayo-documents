Creates an HTML-report with analysis of commit statistics:
- the work pace and number of overtime hours;
- zones of responsibility, number of features and bugs;
- colleagues working style;
- the rate of employee turnover and the makeup of the team;
- location of developers;
- release schedule and vacation calendar;
- cost of features and project as a whole;
- places for refactoring, deleted files, etc.
Links:
Video:
Table of contents
COMMIT STATISTICS REPORT
How to create and view the report?
Using public server
go to the [website]({url})
follow the instructions
run `{code}`
open `{file}`
Use the library NodeJS
Use the library PHP
Use the library Python
Use the library Ruby
Use the library Go
Use source code
download this repository
drop the `{file}` file to the `{dir}`
or drop the `{dir}` folder to your repository (where the `{file}` is located). You can change the name. For example, from `{dir}` to `{to}`.
In this case, it is important that the `{file}` file is generated by the command for offline viewing.
Use github actions
Add [script]({url}) in folder `{dir}` or use this [action]({action}) from the marketplace. A ready, fresh report will be saved in the artifacts.
Use private server
download the [docker image]({url});
run it on your local network;
use the web interface to view the reports, set the URL of the data in the URL parameter `{param}`:
URL of the assayo container, it listens on port {port};
URL of your container with git logs;
By default, the image will run at `{url}`. If it doesn't work, check if port `{port}` is free.
How to concat authors?
In the root directory of your project, you need to create a `{file}` file.
Example of the contents of the file:
Read more about the format of this file you can [here]({url}).
How to export data from git to txt file?
For online viewing
In the root directory of your project run:
For offline viewing
Git will create a file `{file}`. This file contains data for show a report. The difference between the online and offline format is the presence of a wrapper for strings. The offline format will be pulled up like a `{ext}` file if you just opened `{report}`
If you use PowerShell in Windows
By default, the output encoding may not match UTF-8 and the resulting log file will be unreadable. Before saving the log, you can change the encoding with the command.
Or open a saved file and manually change the encoding to UTF-8.
How to view a report on a group of microservices?
Generate for each microservice file `{file}` ({files}, etc.) You can do this manually, or use the [Assayo Crawler]({url}) module for automatic log collection;
See “How to view an online report?”. At the last step, drag all the files at once into the browser window.
See “How to see a report offline?”. At the second step, drag all microservice files ({files}, etc.) to the report folder (`{dir}`).
THE BEST PRACTICES IN THE PROJECT
How to sign commits?
Follow the [Conventional Commits 1.0.0]({url}). Example:
task number in the task tracker `({task})`
type of work `({type}, etc.)`
feature `({page} - new page on site or new function, use one (two) short word or an abbreviation)`
what problem were solved `({text})`
How to add checking for commit message?
Use file {file}
Create file `{file}` in folder `{dir}`
Add this text in file:
Use package [{name}]({url})
Use package {name}
Add in file `{file}` property `{property}`:
Run command `{code}`
ABOUT THIS APP
How to brand the interface?
You can create your own interface theme. Options:
**Title**. You can set default document title in the URL parameter `{property}`. Example: `{url}`
**Visual theme**. To do this, you need to prepare a CSS file with new styles and specify its URL in the `{property}` parameter. Example: `{url}`. You can use class names as selectors. Most of them do not change in new versions.
**Language**. You can set language in the URL parameter `{property}`. Example: `{url}`
**Example:** [demo]({url})
How to rebuild the HTML report from source code?
download this repository `{code}`
the new HTML build will be in the `{dir}` folder
How to add or edit a translation?
You can add a new translation or correct an existing one in the `{dir}` folder.
[Instruction]({url})
️Architecture
General architecture of microservices
[Reports showcase UI]({url}) displays a list of available reports. Each report consists of a title, description, and a list of repositories.
[Crawler service]({url}) collects repository logs for the report.
[Log visualization UI]({url}) **(you here)** displays report. Needs a log file for work.
Feedback, comments
priority method of communication
